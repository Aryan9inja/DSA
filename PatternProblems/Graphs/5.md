Indegree is the number of directed edges on a particular node.

indegree 0 nodes mean we can start from them, there is no dependency on them

if cycle is present we will not be able to push all nodes in topological sort vector

It is called topological sort because for each u->v pair, u comes before v in ordering.