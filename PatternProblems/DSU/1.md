- DSU solves connectivity without traversal
- In this problem specificallyy we needed to count the number of connected components in the graph, which is a common use case for DSU.
- We can use DSU to group nodes into connected components and then count the number of unique components.