### Steps:
- Break problem into smaller subproblems
- same states recomputed repeatedly
- cache solved states
- compute states in dependency order
- store only required previous states

DP is fundamentally about states and transitions

Not every DP can be space optimized.
ONLY when:
future states depend on limited previous states