- This looked DFS at first
- But we can use DP to solve it
- The problem itself is simple
- Get the number of paths to reach the end
- Just add all paths using down and right

- The use of DP as 1 D array in space optimal way is with a caviat
- We are updating the old values in the array
- Making us use i+1 value which is not yet updated